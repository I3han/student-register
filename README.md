# Frontend

open frontend folder separately from a IDE

	run these commands:
		npm install
		ng serve 

# Backend

open backend folder separately from a IDE

	run these commands:
		npm install
		npm test 
