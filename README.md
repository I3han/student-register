Frontend
Angular10
(open frontend folder separately from a IDE)
	run these commands:
		npm install
		ng serve 

Backend
node
(open backend folder separately from a IDE)
	run these commands:
		npm install
		npm test 